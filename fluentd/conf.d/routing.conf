<match app.**>
  @type copy
  @id copy
  <store>
    @type relabel
    @label @REQUEST_STARTED
  </store>
  <store>
    @type relabel
    @label @REQUEST_COMPLETED
  </store>
  <store>
    @type relabel
    @label @LOG
  </store>
</match>
