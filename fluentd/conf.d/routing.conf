<match app.**>
  @type copy
  @id copy
  copy_mode shallow
  <store>
    @type relabel
    @label @REQUEST_STARTED
  </store>
  <store>
    @type relabel
    @label @REQUEST_COMPLETED
  </store>
  <store>
    @type relabel
    @label @LOG
  </store>
</match>
